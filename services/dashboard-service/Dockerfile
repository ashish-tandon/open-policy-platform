FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt || true
COPY src/ ./src/
EXPOSE 9021
CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "9021"]
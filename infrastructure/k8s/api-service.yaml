apiVersion: v1
kind: Service
metadata:
  name: openpolicy-api
  labels:
    app: openpolicy-api
spec:
  selector:
    app: openpolicy-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP